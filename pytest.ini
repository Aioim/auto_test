[pytest]
markers =
    yaml_data(file, group, date, base_dir): Load test parameters from YAML data files
    env: 指定允许运行的环境，例如 @pytest.mark.env('dev', 'staging')
    smoke: 冒烟测试，验证核心功能
    regression: 回归测试，验证已有功能未被破坏
pythonpath = .
testpaths = tests
addopts = -v --strict-markers
